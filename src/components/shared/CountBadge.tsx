
const CountBadge = ({ label }: { label: string | number }) => {
    return (
        <p className='nw-[32px] nh-9 nborder nborder-input  nflex nitems-center njustify-center nrounded-sm '>{label}</p>
    )
}

export default CountBadge